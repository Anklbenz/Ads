# RZD Ads
**RZD Ads** — пакет для интеграции рекламных модулей в Unity-проекты.  

SDK реализован в виде **static-фасада**, что упрощает внедрение в проекты с различными архитектурами
(MVP, MVVM, Clean Architecture и др.).
## Принцип работы

🔹 Для запуска рекламного сервиса необходимо:
```csharp
AdService.Initialize();
```
Метод вызывается один раз при старте приложения.

🔹 Для показа рекламы:
```csharp
AdService.Show();
```
Метод вызывается в том месте, где необходимо отобразить рекламу.

🔹 Инициализация и авторизация
При инициализации сервис:
- отправляет уникальный UUID устройства
- использует уникальный ключ приложения, заданный для конкретного билда
- Авторизация происходит во время вызова Initialize().

🔹 Повторная авторизация
Если авторизация не удалась во время инициализации:
при каждом вызове
```csharp
 AdService.Show()
```
выполняется повторная попытка авторизации.
Это позволяет сервису восстановить работу без перезапуска приложения.

🔹 Если необходимо остановит работу необходимо вызвать:
```csharp
 AdService.Dispose()
```

🔹 Требования
В каждое приложение должен быть добавлен уникальный ключ приложения
Ключ хранится вне SDK (в проекте), чтобы не сбрасываться при обновлении пакета
Сервервр сам следит за частотой показа если показаз не разрешен 

### Установка Через Git URL
Добавьте пакет в Unity через **Package Manager → Add package from Git URL**:

`https://github.com/Anklbenz/Ads.git?path=Assets/Plugins/RZDAds`

## Зависимости
Для корректной работы пакета необходимы следующие зависимости:

1. **Unity TextMeshPro** (версия 3.0.6 и выше)  
   Если в проекте нет Unity подтянет автоматически

2. **UniTask (async/await интеграция)**
UniTask используется для асинхронных операций внутри RZD Ads SDK.

Установите через Git URL:

`https://github.com/Cysharp/UniTask.git?path=src/UniTask/Assets/Plugins/UniTask`

## Структура пакета
- RZDAds/
- package.json      # Описание пакета (name, version, dependencies)
- Runtime/          # Скрипты SDK
- Samples~/         # Примеры использования

## Использование
1. Импортируйте пакет через Git URL
2. Установите зависимость UniTask через Git URL
3. Откройте пример в Samples~

## Обновления
Для установки конкретной версии используйте Udpade в Package Manger
