# RZD Ads
**RZD Ads** — пакет для показа собственной рекламы в Unity-проектах.  

SDK реализован в виде **static-фасада**, что значительно упрощает внедрение в проекты. 
(MVVM, Singleton, Zenject, Custom Di, и др.).

## Принцип работы

🔹 Для запуска рекламного сервиса необходимо вызвать при старте приложения:

```csharp
await AdService.Initialize(string appKey, bool logRequired = false);
appKey - уникальный ключ клиентского приложения
logRequired - включит логгирование
```

При инициализации также:
- Происходит попытка авторизации -> На сервер отправляется уникальный UUID устройства + ключ приложения **appKey**
- Если авторизация не удалась во время инициализации, повторная попытка **авторизации** будет вызвана при каждом вызове AdService.RequestShow()

🔹 Для показа рекламы: 
```csharp
await AdService.RequestShow();
```
Метод делает попытку показать рекламу 
На сервере идет учет времени между показами, показ будет разрешен только через определеннный сервером промежуток времени.
Если сервер не разрешает показ метод завершится  

🔹 Если необходимо остановить работу необходимо вызвать:
```csharp
 AdService.Dispose()
```
Dispose - уничтожит все обьекты скрвиса. После Dispose - возможна повторная инициальзация

🔹 Требования
Каждое приложение должно иметь уникальный ключ **appKey**
Сервер сам следит за частотой показа

🔹 Обновления 
Package Manager -> Выбрать в списке пакетов  RZDAds -> нажать  Update
Будет загружена последняя актуальная версия 

## Зависимости
Для корректной работы пакета необходимы следующие зависимости:

1. **UniTask (async/await интеграция)**
   UniTask используется для асинхронных операций внутри RZD Ads.

2. **Unity TextMeshPro** (версия 3.0.6 и выше)  
   Если в проекте нет Unity подтянет автоматически

### Установка пакета 
1. Импортируйте пакет **RZDAds** через **PackageManager -> + -> Add package from git Url**

`https://github.com/Anklbenz/Ads.git?path=Assets/Plugins/RZDAds`

2. Установите пакет **UniTask** через **PackageManager -> + -> Add package from git Url**
`https://github.com/Cysharp/UniTask.git?path=src/UniTask/Assets/Plugins/UniTask`

3. Пример в Samples показывает работу

## Структура пакета
- RZDAds/
- package.json      # Описание пакета (name, version, dependencies)
- Runtime/          # Скрипты SDK
- Samples~/         # Примеры использования

`https://git.corp/games/adv-sdk.git?path=Assets/Plugins/RZDAds#main` (не работает: Unity не может подтягивать UMP пакеты из GitLab)
