# RZD Ads
**RZD Ads** — пакет для интеграции рекламных модулей в Unity-проекты.  

SDK реализован в виде **static-фасада**, что упрощает внедрение в проекты с различными архитектурами
(MVVM, Singleton, Zenject, Custom Di, Clean Architecture и др.).
## Принцип работы

🔹 Для запуска рекламного сервиса необходимо вызвать при старте приложения:

```csharp
AdService.Initialize(string appKey);
appKey - уникальный ключ клиентского приложения
```

При инициализации также:
- Происходит попытка авторизации -> На сервер отправляется уникальный UUID устройства + ключ приложения **appKey**
- Если авторизация не удалась во время инициализации, повторная попытка **авторизации** будет вызвана при каждом вызове AdSerivice.RequestShow()

🔹 Для показа рекламы: 
```csharp
AdService.RequestShow();
```
Решение показываль ли рекламу принимает сервер. 
На сервере идет учет времени между показами, показ будет разрешен только через определеннный сервером промежуток времени

🔹 Если необходимо остановить работу необходимо вызвать:
```csharp
 AdService.Dispose()
```

🔹 Требования
В каждое приложение должен быть добавлен уникальный ключ приложения appKey
Сервервр сам следит за частотой показа 

## Зависимости
Для корректной работы пакета необходимы следующие зависимости:

1. **Unity TextMeshPro** (версия 3.0.6 и выше)  
   Если в проекте нет Unity подтянет автоматически

2. **UniTask (async/await интеграция)**
UniTask используется для асинхронных операций внутри RZD Ads.


## Структура пакета
- RZDAds/
- package.json      # Описание пакета (name, version, dependencies)
- Runtime/          # Скрипты SDK
- Samples~/         # Примеры использования

### Установка пакета 
1. Импортируйте пакет RZDAds через **PackageManager -> + -> Add packadge from git Url**
`https://git.corp/games/adv-sdk.git?path=Assets/Plugins/RZDAds#main`
`https://github.com/Anklbenz/Ads.git?path=Assets/Plugins/RZDAds`

2. Установите пакет UniTask через **PackageManager -> + -> Add packadge from git Url**
`https://github.com/Cysharp/UniTask.git?path=src/UniTask/Assets/Plugins/UniTask`

3. Пример в Samples показывает работу
